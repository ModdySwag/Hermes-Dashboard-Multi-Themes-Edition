// Placeholder for a built dashboard chunk. The engine's stale-asset repair
// walks /assets/*.{js,css} references in index.html and rewrites any that have
// no file on disk, so the fixture needs one that resolves.
export const fixture = true;
